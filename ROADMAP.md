# SshWeave 路线图

状态：✅ 已完成 · 🚧 进行中 · ⏳ 待处理 · ⚠️ 已阻塞 · 🧪 验证中

| 编号 | 状态 | 任务 | 验收证据 |
| --- | --- | --- | --- |
| SW-1 | ✅ | 建立 .NET 10 Native AOT CLI、V1 配置和系统 OpenSSH 边界 | Release 零警告构建、17 项测试、`ssh -G` 参数检查及 Windows/Linux x64 Native AOT 发布通过 |
| SW-2 | ✅ | 提供 SOCKS5、显式 TCP 映射和进程代理环境 | 参数构建、回环限制、源生成 JSON 和命令冒烟通过 |
| SW-3 | ✅ | 支持活动通道内直接 SSH 到内网设备 | SOCKS5 标准输入/输出桥接与 `ProxyCommand` 生成测试通过 |
| SW-4 | ✅ | 创建只能转发、不能 Shell 的服务端专用用户 | 隔离 Linux `sshd` 验证 `-N -L` 成功且 exec 被拒绝；`nologin`、受限公钥、`MaxSessions 0`、有效配置复核和失败回滚已实现 |
| SW-5 | 🧪 | 使用经过审查的本地 TUN 方案实现透明 TCP 路由 | Windows `0.3.5` 已实现固定哈希数据面、活动路由写入和进程联动清理；路由设置兼容 CIDR、单地址及连续尾部通配，默认 `10.51.*.*` 规范化为 `10.51.0.0/16`，默认路由、非连续通配、地址重叠及既有路由冲突仍拒绝。47 项测试和两个 `win-x64` Native AOT 发布通过，待管理员现场 VNC 直连、异常清理及 Linux 实现验收 |
| SW-6 | ⚠️ | 提供真实 ICMP 和通用 UDP 的可选 L3 模式 | 依赖 `PermitTunnel` 及远端返回路由或限定 NAT；未获得服务器变更验收前不得启用 |
| SW-7 | 🧪 | 增加安装包、代码签名、发行校验和端到端 SSH 测试 | WiX 6 已生成 8,011,776 字节的 `SshWeave 0.3.5` x64 MSI，SHA-256 为 `185C00BF78AC62F4A807D68209FEF2262D67C05FC16F5A1465DEA2624C4548D7`；Native AOT、管理员清单门禁、WiX 构建及 9 个应用载荷的行政解包通过，仍待代码签名、目标机安装/升级/卸载和现场直连验收 |
| SW-8 | 🧪 | 使用 MewUI 实现 Windows Native AOT 桌面控制台、托盘、认证与可观测会话 | `0.3.5` 桌面入口启动即请求管理员权限，并在路由设置中支持 `10.*.*.*`、`10.165.*.*` 等透明 TCP 范围；7,522,304 字节桌面 Native AOT EXE 的 SHA-256 为 `7081C6ECB8B6567FE631611616BF9FFD0757753396005F9FFBCAF13774A3EF6E`。5,965 字节焯烟 TCP 直通文件 SHA-256 为 `E2F7C81E8E8281F48D5C86953EC96A7843A4F96A6939D36B14EEC0C64917900A`，已离线确认 `10.51.*.*`、无端口映射、严格主机校验且未保存口令，仍待管理员实机 VNC 直连和托盘人工验收 |

## SW-5 下一步验证

2026-08-15 已完成第 1 项的候选生成与离线复核。验证包位于
`artifacts/packages/SshWeave-SW5-preview-20260815-win-x64.zip`，包含当前用户安装/卸载、
脱敏配置样例、中文验证清单和分阶段证据采集脚本；SshWeave 二进制尚未代码签名，因此只用于
现场验证。以下第 2 至 5 项仍须由管理员在真实环境执行。

1. 以 `win-x64` 发布脚本生成候选，复核 `sshweave`、`tun2socks.exe`、`wintun.dll`、
   `THIRD-PARTY-NOTICES.md` 和 `WINTUN-LICENSE.txt` 的文件清单与 SHA-256。
2. 双击焯烟 TCP 直通连接文件，确认 UAC 在桌面程序入口出现；启用默认 `10.51.*.*` 后，
   确认其规范化为 `10.51.0.0/16`，且 `SshWeave` Wintun 网卡使用
   `198.18.0.1/30`，且只新增目标 CIDR 的活动路由，不修改默认路由、远端路由或 NAT。
3. 保持 SSH 会话后使用 VNC 客户端直接连接 `10.51.11.132` 的现场 VNC 端口，记录握手、
   画面、连接数及双向字节证据；不得设置本地端口映射、SOCKS5 或 `ProxyCommand`。
4. 主动断开、SSH 异常退出和 `tun2socks` 异常退出各执行一次，确认目标路由与 TUN 地址均被
   清理；再验证现有同前缀路由会默认拒绝覆盖，进程重启可清理同名网卡上的遗留活动路由。
5. 确认真实 ICMP 和通用 UDP 仍不可用，不能把用户态响应当作目标设备 `ping`；完成 Windows
   证据后再实现 Linux TUN 路由，取得两平台证据后才能把 SW-5 标记为 ✅。
